using System;
using System.Collections.Generic;
using System.Text;

namespace _18Ghosts
{
    class Setup
    {
        public void DefinePosition(Ghost ghost, Board board, Player player)
        {
            for (int i = 0; i < length; i++)
            {
                if (savedGhost.color == ConsoleColor.Blue &&
                board.type[wantedPosition.Row, wantedPosition.Column]
                == RoomType.Blue && board.state[wantedPosition.Row,
                wantedPosition.Column] == RoomState.Undecided)

                if (savedGhost.color == ConsoleColor.Red &&
                board.type[wantedPosition.Row, wantedPosition.Column]
                == RoomType.Blue && board.state[wantedPosition.Row,
                wantedPosition.Column] == RoomState.Undecided)

                if (savedGhost.color == ConsoleColor.Yellow &&
                board.type[wantedPosition.Row, wantedPosition.Column]
                == RoomType.Blue && board.state[wantedPosition.Row,
                wantedPosition.Column] == RoomState.Undecided)
            }
        }

        public void PercorrerJogadores(Board board, Ghost ghost) 
    }
}
